import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  container: {
    paddingTop: 40,
    paddingBottom: 40,
    display: 'flex',
    alignItems: 'center',
  },
});
